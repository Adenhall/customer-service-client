import { createMuiTheme } from "@material-ui/core/styles";

/** Theme for the app. **Note** Remember to add type defs in src/createMuiTheme.d.ts */
export default createMuiTheme({
  palette: {
    common: {
      stayrOrange: "#fe6962",
    },
  },
});
